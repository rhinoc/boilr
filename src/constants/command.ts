import { EXTENSION_NAMES } from './name';

/**
 * @related package.json
 */
export const COMMAND = {
  NEW_FILE: `extension.${EXTENSION_NAMES.kebabCase}.new_file`,
  CREATE_BOILERPLATE: `extension.${EXTENSION_NAMES.kebabCase}.create_boilerplate`,
};
